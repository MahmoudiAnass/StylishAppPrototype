<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FCF3F6" /> <!-- Background color -->
            <corners android:radius="50dp" /> <!-- Optional: Rounded corners -->
        </shape>
    </item>

    <!-- Border -->
    <item android:top="2dp" android:left="2dp" android:right="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Transparent to show the background -->
            <stroke
                android:width="2dp"
                android:color="#F83758" /> <!-- Border color -->
            <corners android:radius="50dp" /> <!-- Optional: Rounded corners -->
        </shape>
    </item>
</layer-list>